apiVersion: v1
kind: Namespace
metadata:
  name: interviews-tv
  labels:
    name: interviews-tv
    environment: production
---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: interviews-tv-quota
  namespace: interviews-tv
spec:
  hard:
    requests.cpu: "10"
    requests.memory: 20Gi
    limits.cpu: "20"
    limits.memory: 40Gi
    persistentvolumeclaims: "10"
    services: "10"
    secrets: "10"
    configmaps: "10"
---
apiVersion: v1
kind: LimitRange
metadata:
  name: interviews-tv-limits
  namespace: interviews-tv
spec:
  limits:
  - default:
      cpu: "1"
      memory: "1Gi"
    defaultRequest:
      cpu: "100m"
      memory: "128Mi"
    type: Container
